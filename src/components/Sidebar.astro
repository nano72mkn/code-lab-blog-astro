---
import Author from "./Author.astro";
import Sns from "./Sns.astro";
---

<aside class="space-y-5 xl:w-1/4">
  <Author />

  <Sns />
  <!-- {tocData && (
        <div class="md:sticky md:top-10 p-10 shadow-md rounded-md bg-white">
          <div class=" text-lg font-bold mb-5">もくじ</div>
          <ul class="space-y-3 pl-5">
            {tocData.map((toc, index) => (
              <li
                key={index}
                class={`${
                  toc.lvl === 2
                    ? 'pl-1'
                    : toc.lvl === 3
                    ? 'pl-4 text-gray-600'
                    : ''
                } ${toc.lvl === 1 ? 'list-disc' : ''}`}
              >
                <a
                  href={`#${toc.slug}`}
                  class={`text-sm ${toc.lvl === 1 && 'font-bold'}`}
                >
                  {toc.content}
                </a>
              </li>
            ))}
          </ul>
        </div>
      )} -->
</aside>
