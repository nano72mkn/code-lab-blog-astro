---
const props = Astro.props;

const { class: className } = props;

// ${name ? "rounded-t-none" : ""}
---

<pre class={`text-sm p-5 rounded-md bg-sky-950 text-white ${className}`}>
  <slot />
</pre>
